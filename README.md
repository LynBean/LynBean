```java
int heath = 100;
☕ getCoffee() { return ☕; }

while (isAwake() && isAlive()) {  
  if (getCoffee() == null) {
    health -= 20;
  }
  else {
    health += 20;
  }
  work(12 * 60 * 60); // 12 hours
  family(4 * 60 * 60); // 4 hours
  sleep(8 * 60 * 60); // 8 hours

  if (health <= 0) {
    isFreedom = true;
    break;
  }
}
```
